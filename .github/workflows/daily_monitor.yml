name: 东风舆情监测日报

on:
  schedule:
    # UTC时间，需要减8小时
    # 06:00 CST = 22:00 UTC (前一天)
    - cron: '0 22 * * *'
    # 07:00 CST = 23:00 UTC (前一天)
    - cron: '0 23 * * *'
    # 08:00 CST = 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write

jobs:
  monitor:
    name: 采集并分析舆情
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 验证配置文件
        run: |
          echo "🔍 检查配置文件..."
          
          if [ ! -f config/models.yaml ]; then
            echo "❌ 错误: config/models.yaml 不存在"
            exit 1
          fi
          
          if [ ! -f config/keywords.yaml ]; then
            echo "❌ 错误: config/keywords.yaml 不存在"
            exit 1
          fi
          
          if [ ! -f config/sources.yaml ]; then
            echo "❌ 错误: config/sources.yaml 不存在"
            exit 1
          fi
          
          echo "✅ 配置文件检查通过"

      - name: 运行监测程序
        env:
          DASHSCOPE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          echo "🚀 开始采集数据..."
          python src/main.py --mode full
          echo "✅ 任务完成"

      - name: 提交数据更新
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'
          git add -A
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto update: 舆情数据 $(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M')" && git push)
